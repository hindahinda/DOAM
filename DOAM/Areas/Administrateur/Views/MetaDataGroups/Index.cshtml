@model IEnumerable<MyApp.Application.ViewModels.MetaDataGroupViewModel>

@{
    ViewBag.Title = "Index";
      //Layout = "~/Views/Shared/_Layout.cshtml";
       Layout = "~/Views/Shared/Layout_AreaAdmin.cshtml";
}
    <div class="site-section bg-light table-responsive-sm">
        <div class="container">

            <h2>Index</h2>

            <p>
                @Html.ActionLink("Create New", "Create")
            </p>
            <table class="table table-dark table-striped">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.GroupName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Description)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.GroupName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Description)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.MetaDataGroupID }) |
                            @Html.ActionLink("Details", "Details", new { id = item.MetaDataGroupID }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.MetaDataGroupID })
                        </td>
                    </tr>
                }

            </table>
</div></div>