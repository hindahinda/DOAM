@model IEnumerable<MyApp.Infrastructure.DB.AssetTypeSupportedMetaData>

@{
    ViewBag.Title = "Index";
     //Layout = "~/Views/Shared/_Layout.cshtml";
       Layout = "~/Views/Shared/Layout_AreaAdmin.cshtml";
}

    <div class="site-section bg-light table-responsive-sm">
        <div class="container">
            <h2>Index</h2>

            <p>
                @Html.ActionLink("Create New", "Create")
            </p>
            <table class="table table-dark table-striped">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.AssetType.TypeLabel)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.MetaData.Title)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.AssetType.TypeLabel)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.MetaData.Title)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.AssetSupportedMetaDataID }) |
                            @Html.ActionLink("Details", "Details", new { id = item.AssetSupportedMetaDataID }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.AssetSupportedMetaDataID })
                        </td>
                    </tr>
                }

            </table>
</div></div>