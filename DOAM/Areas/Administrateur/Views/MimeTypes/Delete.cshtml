@model MyApp.Application.ViewModels.MimeTypeViewModel

@{
    ViewBag.Title = "Delete";
     
       Layout = "~/Views/Shared/Layout_AreaAdmin.cshtml";
}

    <div class="site-section bg-light">
        <div class="container">
            <h2>Delete</h2>

            <h3>Are you sure you want to delete this?</h3>
            <div>
                <h4>MimeType</h4>
                <hr />
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.Mime)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Mime)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(model => model.AssetType.TypeLabel)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.AssetType.TypeLabel)
                    </dd>

                </dl>

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color">
                        <input type="submit" value="Delete" class="btn btn-default" /> |
                        @Html.ActionLink("Back to List", "Index")
                    </div>
                }
            </div>
</div></div>